image: node:10.18.0

stages:
  - deploy

deploy job:
  stage: deploy
  script:
    - apt-get update -y
    - apt-get -y install rsync
    - echo "$WETRADE_SSH_PEM" > "$(pwd)/wetrade.pem"
    - cat ./wetrade.pem
    - rsync --delete -azvv -e "ssh -i ./wetrade.pem" ./dist/qa/* root@47.75.61.170:/www/wwwroot/system.cangshu360.com/